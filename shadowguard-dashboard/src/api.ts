import { alerts, applications, overview, profiles } from './mock';
import type { Alert, AppInventory, Overview, Page, UserProfile } from './types';
const useMocks = import.meta.env.VITE_USE_MOCKS !== 'false';
const baseUrl = import.meta.env.VITE_API_URL ?? 'http://localhost:8000/api/v1';
const isRecord=(x:unknown):x is Record<string,unknown> => typeof x==='object' && x!==null;
async function request<T>(path:string, fallback:T):Promise<T>{ if(useMocks){ await new Promise(r=>setTimeout(r,220)); return fallback; } const response=await fetch(`${baseUrl}${path}`,{credentials:'include',headers:{Accept:'application/json'}}); if(!response.ok) throw new Error(`Request failed (${response.status})`); const data:unknown=await response.json(); if(!isRecord(data) && !Array.isArray(data)) throw new Error('Invalid API response'); return data as T; }
export const api={ overview:()=>request<Overview>('/overview',overview), alerts:async(query:{page:number;search:string;severity:string;status:string})=>{const rows=alerts.filter(a=>(!query.search||`${a.userName} ${a.app}`.toLowerCase().includes(query.search.toLowerCase()))&&(!query.severity||a.severity===query.severity)&&(!query.status||a.status===query.status));const pageSize=5;const params=new URLSearchParams({page:String(query.page),search:query.search,severity:query.severity,status:query.status});return request<Page<Alert>>(`/alerts?${params}`, {items:rows.slice((query.page-1)*pageSize,query.page*pageSize),page:query.page,pageSize,total:rows.length});}, alert:(id:string)=>{const item=alerts.find(a=>a.id===id);if(!item) return Promise.reject(new Error('Alert not found'));return request<Alert>(`/alerts/${id}`,item);}, user:(id:string)=>{const item=profiles[id];if(!item)return Promise.reject(new Error('User not found'));return request<UserProfile>(`/users/${id}`,item);}, apps:()=>request<AppInventory[]>('/applications',applications), exportRequest:()=>request<{requestId:string}>('/exports',{requestId:'EXP-2026-0810'}) };
